##
## libpq: si auth-sql.conf usa connect sin password= (por #/= en el secreto y el
## parser de Dovecot), el password va en PGPASSWORD. Los auth workers no heredan
## todo el entorno; sin esto, el driver pgsql abre conexión sin clave.
##
import_environment = TZ PGPASSWORD
